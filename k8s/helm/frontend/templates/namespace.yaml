# templates/namespace.yaml
apiVersion: v1
kind: Namespace
metadata:
  name: {{ .Values.namespace }}
  labels:
    {{- include "frontend.labels" . | nindent 4 }}
    # This label is used by kubectl to filter namespaces belonging to this platform
    platform: customer-sales